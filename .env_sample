# Gas & Energy Mechanics Copilot — Environment Configuration
# Source this file before running: source .env

# AWS Profile (for Bedrock: GPT-4o on Bedrock + Titan Embeddings V2)
export AWS_PROFILE="vscode-user"

# Application Configuration
export CONFIG_DIR="./config"

# CrewAI / LiteLLM — LLM for both agents (via Amazon Bedrock)
export MODEL="bedrock/openai.gpt-oss-120b-1:0"
